[settings]
lockfile_platforms = [] # all here even if reduced global set locally

[tools]
committed = "1.1.11"
dprint = "0.55.1"
lefthook = "latest"
ruff = "0.15.20"
rumdl = "0.2.12"
typos = "1.47.2"

[env]
NOX_DEFAULT_VENV_BACKEND = "uv"
